interface:
  display_name: "Validation"
  short_description: "Evidence-based browser validation"
  default_prompt: "Use $validation with no options to run the complete standard browser cycle; use --full for exhaustive release-depth validation."
